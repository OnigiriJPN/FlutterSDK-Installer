<Application x:Class="FlutterSDKInstaller.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:FlutterSDKInstaller"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <!-- カラーパレット・ブラシの定義 -->
        <SolidColorBrush x:Key="ButtonBgBrush" Color="#E1E1E1"/>
        <SolidColorBrush x:Key="ButtonBorderBrush" Color="#ADADAD"/>
        <SolidColorBrush x:Key="ButtonFgBrush" Color="#000000"/>

        <SolidColorBrush x:Key="AccentColorBrush" Color="#0078D4"/>
        <SolidColorBrush x:Key="ButtonHoverBgBrush" Color="#E5F1FB"/>
        <SolidColorBrush x:Key="ButtonHoverBorderBrush" Color="#0078D4"/>

        <SolidColorBrush x:Key="ButtonPressedBgBrush" Color="#CCE4F7"/>
        <SolidColorBrush x:Key="ButtonPressedBorderBrush" Color="#005499"/>

        <SolidColorBrush x:Key="DisabledBgBrush" Color="#F0F0F0"/>
        <SolidColorBrush x:Key="DisabledBorderBrush" Color="#D9D9D9"/>
        <SolidColorBrush x:Key="DisabledFgBrush" Color="#A0A0A0"/>

        <SolidColorBrush x:Key="ErrorTextBrush" Color="#D13438"/>

        <SolidColorBrush x:Key="PrimaryButtonBgBrush" Color="#0078D4"/>
        <SolidColorBrush x:Key="PrimaryButtonFgBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="PrimaryButtonBorderBrush" Color="#005A9E"/>
        <SolidColorBrush x:Key="PrimaryButtonHoverBgBrush" Color="#106EBE"/>

        <!-- 通常の角丸ボタンスタイル -->
        <Style x:Key="RoundedButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource ButtonBgBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource ButtonFgBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource ButtonBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="10,2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="4" SnapsToDevicePixels="True">
                            <ContentPresenter x:Name="contentPresenter" 
                                              Focusable="False" 
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                              Margin="{TemplateBinding Padding}" 
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <!-- マウスオーバー時（トリガーセッターで安全に切り替え） -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource ButtonHoverBgBrush}"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource ButtonHoverBorderBrush}"/>
                            </Trigger>

                            <!-- プレス時 -->
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource ButtonPressedBgBrush}"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource ButtonPressedBorderBrush}"/>
                            </Trigger>

                            <!-- 非活性時 -->
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource DisabledBgBrush}"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource DisabledBorderBrush}"/>
                                <Setter Property="Foreground" Value="{StaticResource DisabledFgBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 推奨ボタン（Primary）用のスタイル -->
        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource RoundedButton}">
            <Setter Property="Background" Value="{StaticResource PrimaryButtonBgBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryButtonFgBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryButtonBorderBrush}"/>
        </Style>
    </Application.Resources>
</Application>
